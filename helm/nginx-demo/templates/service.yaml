apiVersion: v1
kind: Service
metadata:
  name: nginx-demo-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: nginx-demo
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
